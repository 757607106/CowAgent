[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: cross-module integration tests
    e2e: end-to-end tests with real subprocesses or HTTP servers
    docker: tests that require Docker Engine and Docker Compose
addopts = -ra
